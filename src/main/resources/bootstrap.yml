spring:
  profiles:
    active: ${ACTIVE_PROFILE:local}
  application:
    name: mst-order-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:9999}
    consul:
      enabled: true
      host: 127.0.0.1
      port: 8500
      ribbon:
        enabled: true
      discovery:
        enabled: true
        register: true
